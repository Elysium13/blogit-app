<!DOCTYPE html>
<html>
<head>
  <title>Blogin</title>
  <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">BlogIn</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active"><%= link_to 'Blogs', '/blog'%></li>
        <li><% if user_signed_in? %>
            <%= link_to "<button class='btn btn-xs btn-success navbar-btn'><span class='glyphicon glyphicon-plu-sign'></span>New Blog Post</button>".html_safe, "/blog/posts/new" %>
            <% end %></li>
    </ul>
      
      <ul class="nav navbar-nav navbar-right">
         <% if user_signed_in? %>
        <li><p class="navbar-text">Signed in as <span class="bUser"><%= current_user.email %></span></p></li>
        <li><%= link_to "<button class='btn btn-xs btn-danger navbar-btn btnNav'>Sign Out</button>".html_safe, "/users/sign_out" ,method: :delete %></li>
    <% else %>
        <li><%= link_to "<button class='btn btn-xs btn-info navbar-btn btnNav'>Register</button>".html_safe, "/users/sign_up" %></li>
        <li><%= link_to "<button class='btn btn-xs btn-success navbar-btn btnNav'>Sign In</button>".html_safe, "/users/sign_in" %></li>
    <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
<div class="Mid">
	<%= yield %>
</div>
</body>
</html>
